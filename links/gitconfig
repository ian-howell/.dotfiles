[core]
    editor = nvim
    excludesfile = ~/.dotfiles/links/global_gitignore
    pager = delta
[push]
    default = matching
[remote.origin]
    # Only push the current branch
    push = HEAD
[include]
    path = ~/.dotfiles/local_gitconfig
[user]
    email = ian.howell0@gmail.com
    name = Ian Howell
[diff]
    algorithm = histogram
    colorMoved = default
[commit]
    verbose = true
[init]
    defaultBranch = main
[interactive]
    diffFilter = delta --color-only --features=interactive
[delta]
    navigate = true    # use n and N to move between diff sections
    side-by-side = true
    line-numbers = true
[delta "interactive"]
    keep-plus-minus-markers = false
[merge]
    conflictstyle = diff3
[color]
    ui = true
[color "diff-highlight"]
    oldNormal = red bold
    oldHighlight = red bold 52
    newNormal = green bold
    newHighlight = green bold 22
[color "diff"]
    meta = 11
    frag = magenta bold
    func = 146 bold
    commit = yellow bold
    old = red bold
    new = green bold
    whitespace = red reverse
[alias]
    clean-branches = clean-branches


; Only work stuff goes below this line
[credential]
    ; helper = /usr/local/bin/git-credential-manager
    ; Use the windows helper
    helper = /mnt/c/Program\\ Files/Git/mingw64/bin/git-credential-manager.exe
    azreposCredentialType = oauth
    ; This one doesn't seem to work
    msauthflow = devicecode
    useHttpPath = true
[url "https://dev.azure.com/mariner-org/ECF/_git/cluster-api-provider-kubevirt"]
    insteadOf = https://dev.azure.com/mariner-org/ECF/cluster-api-provider-kubevirt
    insteadOf = https://dev.azure.com/mariner-org/ECF/cluster-api-provider-kubevirt.git
[url "https://"]
    ; These prevent go from hanging. You should have the following settings to force interactivity
    ; export GCM_INTERACTIVE=auto
    ; export GIT_TERMINAL_PROMPT=1
    insteadOf = git://
    insteadOf = ssh://
    insteadOf = git+ssh://
[credential "azrepos:org/msazuredev"]
    username = ianhowell@microsoft.com
    azureAuthority = https://login.microsoftonline.com/72f988bf-86f1-41af-91ab-2d7cd011db47
[credential "azrepos:org/mariner-org"]
    azureAuthority = https://login.microsoftonline.com/72f988bf-86f1-41af-91ab-2d7cd011db47
    username = ianhowell@microsoft.com
