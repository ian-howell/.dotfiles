-- mini.icons - icon provider used by fyler.nvim and snacks.nvim pickers.
-- setup() defines the global `MiniIcons`, which snacks looks up directly; fyler
-- reaches it by module require. Defaults are fine, so no options are passed.
require("mini.icons").setup()
