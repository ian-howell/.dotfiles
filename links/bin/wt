#!/usr/bin/env bash
set -euo pipefail

git_common_dir="$(git rev-parse --path-format=absolute --git-common-dir)"
repo_root="$(dirname "$git_common_dir")"

if [ $# -gt 0 ]; then
    branch="$1"
else
    # Show all branches (local + remote), strip 'remotes/origin/' prefix,
    # deduplicate, strip leading whitespace/markers, let user pick with fzf.
    branch=$(git branch -a --color=always \
        | sed 's|remotes/origin/||' \
        | sed 's|^[[:space:]]*[+*[:space:]]*||' \
        | sort -u \
        | fzf --ansi \
              --height=100% \
              --layout=reverse \
              --preview-window='bottom,60%' \
              --prompt="branch> " \
              --preview='
                wt_dir="'"$repo_root"'/.worktrees/{1}"
                if git -C "$wt_dir" rev-parse --git-dir >/dev/null 2>&1; then
                  echo "worktree: $wt_dir"
                  echo
                  git -C "$wt_dir" status --short
                else
                  git log --oneline --color=always -20 {1} 2>/dev/null \
                    || git log --oneline --color=always -20 origin/{1}
                fi
              ')
fi

# If empty, fall back to current branch
if [ -z "$branch" ]; then
    if ! branch=$(git rev-parse --abbrev-ref HEAD); then
        echo "Error: Not in a git repository or unable to determine current branch" >&2
        exit 1
    fi
fi

# 1. Ensure local branch exists
if ! git rev-parse --verify "$branch" >/dev/null 2>&1; then
    # Check if a remote-tracking ref exists
    if git rev-parse --verify "refs/remotes/origin/$branch" >/dev/null 2>&1; then
        echo "Creating local branch $branch tracking origin/$branch"
        git branch "$branch" "refs/remotes/origin/$branch"
    else
        echo "Creating new local branch $branch"
        git branch "$branch"
    fi
fi

# 2. Ensure upstream is set correctly
if git rev-parse --verify "refs/remotes/origin/$branch" >/dev/null 2>&1; then
    current_upstream="$(git rev-parse --abbrev-ref "$branch@{upstream}" 2>/dev/null || true)"
    if [ "$current_upstream" != "origin/$branch" ]; then
        echo "Setting upstream for $branch → origin/$branch"
        git branch --set-upstream-to="origin/$branch" "$branch"
    fi
fi

# 3. Ensure worktree exists
worktree_dir="$repo_root/.worktrees/$branch"
if ! git -C "$worktree_dir" rev-parse --git-dir >/dev/null 2>&1; then
    echo "Creating worktree $worktree_dir"
    git worktree add "$worktree_dir" "$branch"
fi

# 4. Ensure tmux session exists and switch to it.
# Session format: "repo 󰘬 branch" using thin spaces (U+2009) around the
# nf-md-git glyph (U+F062C). Thin spaces are NOT regular spaces — they avoid
# word-splitting in unquoted shell expansions while keeping the name readable
# in tmux status bars and pickers.
repo_name="$(basename "$repo_root")"
session="${repo_name} 󰘬 ${branch}"
tmux-switch "$session" --dir "$worktree_dir"
