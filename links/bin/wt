#!/usr/bin/env bash
set -euo pipefail

git_common_dir="$(git rev-parse --path-format=absolute --git-common-dir)"
repo_root="$(dirname "$git_common_dir")"
# Strip the leading dot for aesthetics - tmux will replace it with '_' otherwise
repo_name="$(basename "$repo_root")"
repo_name="${repo_name#.}"

# ── Argument parsing ──────────────────────────────────────────────────────────
mode="create"
if [ "${1:-}" = "-d" ]; then
    if [ -z "${2:-}" ]; then
        echo "Error: -d requires a branch name" >&2
        echo "Usage: wt -d <branch>" >&2
        exit 1
    fi
    mode="delete"
    branch="$2"
elif [ "${1:-}" = "-l" ]; then
    git worktree list --porcelain \
        | awk '
            /^worktree / { wt = substr($0, 10) }
            /^branch /   { br = substr($0, 8); sub("^refs/heads/", "", br) }
            /^$/         { if (wt != "" && wt != "'"$repo_root"'") print wt "\t" br; wt=""; br="" }
            END          { if (wt != "" && wt != "'"$repo_root"'") print wt "\t" br }
          '
    exit 0
elif [ $# -gt 0 ]; then
    branch="$1"
else
    # Show existing worktrees (excluding the main checkout), let user pick with fzf.
    worktree_lines=$(wt -l)
    if [ -z "$worktree_lines" ]; then
        echo "Error: no worktrees found (use 'wt <branch>' to create one)" >&2
        exit 1
    fi
    branch=$(echo "$worktree_lines" \
        | fzf --ansi \
              --height=100% \
              --layout=reverse \
              --preview-window='bottom,60%' \
              --prompt="worktree> " \
              --header='ctrl-x: delete' \
              --with-nth=2 \
              --delimiter='\t' \
              --bind 'ctrl-x:execute-silent(wt -d {2})+reload-sync(wt -l)' \
              --preview='
                wt_dir={1}
                echo "==> git status"
                git -C $wt_dir status
                echo
                echo
                echo "==> recent commits"
                git -C $wt_dir log --oneline --color=always -20
              ' \
        | cut -f2)
fi

# If empty (e.g. fzf cancelled), exit cleanly
if [ -z "${branch:-}" ]; then
    exit 0
fi

# First check if there's an existing worktree. If there is, we should use the
# existing directory. This is important because AI agents like to invent their
# own naming patterns (rather than just using `wt`), which breaks the normal
# pattern and causes breakage.
worktree_dir="$(git worktree list --porcelain | awk -v b="refs/heads/$branch" '
    /^worktree / { wt = substr($0, 10) }
    /^branch /   { if (substr($0, 8) == b) { print wt; exit } }
')"
# If there isn't an existing directory, we fallback to the standard pattern.
if [ -z "$worktree_dir" ]; then
    worktree_dir="$repo_root/../$repo_name.worktrees/$branch"
fi

# Session format: "repo⋅󰘬⋅branch" using Dot Operator (smaller) (U+22C5) around
# the nf-md-git glyph (U+F062C). Dots avoid word-splitting in unquoted shell
# expansions while keeping the name readable in tmux status bars.
session="${repo_name}⋅󰘬⋅${branch}"

# Periods are replaced with underscores — tmux does this silently, so we apply
# it explicitly for consistency.
session="${session//./_}"

# ── Delete mode ───────────────────────────────────────────────────────────────
if [ "$mode" = "delete" ]; then
    # First, make sure we're at the root of the repo. We may have started in the
    # worktree directory that we're trying to delete, and it's not really possible
    # to delete a worktree while in it.
    cd "$repo_root"

    # Remove the worktree.
    git worktree remove --force "$worktree_dir"

    # Delete the branch.
    git branch -D "$branch"

    current_session="$(tmux display-message -p '#S' 2>/dev/null || true)"
    if [ -n "$current_session" ] && [ "$session" = "$current_session" ]; then
        # We're inside the session we're about to kill - switch away first.
        other_session="$(tmux list-sessions -F '#S' 2>/dev/null | grep -v "^${session}$" | head -1 || true)"
        if [ -n "$other_session" ]; then
            tmux switch-client -t "=$other_session"
        else
            tmux detach-client
        fi
    fi
    # Kill the tmux session.
    tmux kill-session -t "=$session"

    exit 0
fi

# ── Create / switch mode ──────────────────────────────────────────────────────

# 1. Ensure local branch exists
if ! git rev-parse --verify "$branch" >/dev/null 2>&1; then
    # Check if a remote-tracking ref exists
    if git rev-parse --verify "refs/remotes/origin/$branch" >/dev/null 2>&1; then
        echo "Creating local branch $branch tracking origin/$branch"
        git branch "$branch" "refs/remotes/origin/$branch"
    else
        echo "Creating new local branch $branch"
        git branch "$branch"
    fi
fi

# 2. Ensure upstream is set correctly
if git rev-parse --verify "refs/remotes/origin/$branch" >/dev/null 2>&1; then
    current_upstream="$(git rev-parse --abbrev-ref "$branch@{upstream}" 2>/dev/null || true)"
    if [ "$current_upstream" != "origin/$branch" ]; then
        echo "Setting upstream for $branch → origin/$branch"
        git branch --set-upstream-to="origin/$branch" "$branch"
    fi
fi

# 3. Ensure worktree exists
if ! git -C "$worktree_dir" rev-parse --git-dir >/dev/null 2>&1; then
    echo "Creating worktree $worktree_dir"
    git worktree add "$worktree_dir" "$branch"
fi

# 4. Ensure tmux session exists and switch to it.
tmux-switch "$session" --dir "$worktree_dir"
