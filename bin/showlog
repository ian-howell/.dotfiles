#!/bin/bash
if [ $# -ne 2 ]; then
	echo "Usage: showlog <app_name> <log_type>"
	exit
fi

unset -v latest
for file in /opt/app/osaaf/logs/$1/*$2*; do
    [[ $file -nt $latest ]] && latest=$file
done
tail -n 60 -f $latest
